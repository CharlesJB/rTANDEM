
## Use the R_HOME indirection to support installations of multiple R version
PKG_LIBS = -LC:/Expat-2.1.0_32/Bin -LC:/expat-2.1.0 -shared -lexpat -lpthread $(shell "${R_HOME}/bin/Rscript.exe" -e "Rcpp:::LdFlags()")
