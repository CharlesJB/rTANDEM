
## Use the R_HOME indirection to support installations of multiple R version
PKG_LIBS = -L$(LIB_EXPAT)$(R_ARCH)/lib -shared -lexpat -lpthread $(shell "${R_HOME}/bin/Rscript.exe" -e "Rcpp:::LdFlags()")
